# 个人网站 · 过程说明

> 本文件对应任务书要求的「过程说明」，记录网站从构想到上线的决策与思考。

## 0. 项目概览

- 用纯 HTML / CSS / JavaScript 制作的个人信息站，采用「作品集 × Playground 小玩具」合体定位，托管于 GitHub Pages。
- 仓库地址：（部署后填写，形如 `https://github.com/helloword_sleep/helloword-sleep`）
- 网站地址：（部署后填写，形如 `https://helloword_sleep.github.io/helloword-sleep/`）

## 1. 网站定位和设计目标

- **定位**：个人作品集 × 小玩具 Playground 合体站。
- **受众**：同行 / 招聘方 / 爱鼓捣的技术爱好者。
- **设计目标**：
  - 只展示「产出物」，不写私人心路，不碰公司业务；边界完全自己控。
  - 网站本身是可展示的第一个真实项目。
  - 练熟 Git / GitHub 全流程（初始化 → 提交 → 推送 → Pages 上线）。
  - 手机 + 电脑都能正常浏览（响应式）。
- **明确排除**：学习笔记 / 成长记录主题。原因：涉及过多个人与公司资料，既不够尊重自己也不安全；经与 AI 讨论后放弃，改为「只展示产出」的合体方案。

## 2. 技术栈及选型理由

- **选定**：纯 HTML / CSS / JavaScript（无框架、无构建步骤、无后端）。
- **选型理由**（对应任务"理解并取舍 AI 生成代码"的要求）：
  1. 零抽象：写的就是浏览器跑的，每行可读，能改、能取舍。
  2. 无构建：写文件 → `git push` → Pages 直接上线，发布链路最短最透明。
  3. Git 最干净：无 `node_modules` / `lockfile` / 构建产物，每次提交就是"我改了这一页"。
  4. Pages 原生零配置：静态文件直接托管，不需要 CI / 构建 Action。
  5. 调试直白、永不过时：无框架版本漂移，遇到问题时看到的就是真实元素。
- **对比排除**：
  - 静态生成器（Jekyll / Hugo）：为 5 个页面上模板系统属过度设计，且离"理解代码"更远。
  - 现代框架（React / Next / Vue / Spring Boot）：抽象多、Pages 需 build 后才能部署、不利于"理解并修改代码"。
- **托管**：GitHub Pages（免费静态托管，恰契合纯静态、无后端）。

## 3. 主要页面和交互逻辑设计思路

- **页面结构（多页）**：
  - `index.html` 首页：hero 介绍 + 卡片入口。
  - `about.html` 关于：极简，不写真名 / 公司 / 住址。
  - `projects.html` 作品：放本站等真实项目，附源码链接。
  - `playground.html` Playground：小玩具列表。
  - `contact.html` 联系：GitHub 链接为主，邮箱待定。
- **视觉风格**：干净浅底 + 卡片式布局 + 单一靛蓝强调色 `#6366f1`；系统字体栈（零加载、最快最稳）。
- **明暗主题**：双主题开关。首次访问跟随系统 `prefers-color-scheme`，之后用 `localStorage` 记忆用户选择；用 CSS 变量实现，明暗各一套取值。
- **导航与交互**：
  - 顶部 sticky 导航栏。
  - 桌面：链接横排；手机（≤640px）：汉堡菜单 ☰ 收起 / 展开。
  - Playground：点卡片跳独立玩具页面（数独、猜飞机），保证 9×9 等布局有整屏空间。
  - 主题切换按钮：🌓 一键切换并记住偏好。
- **响应式**：flexbox / grid + 媒体查询，手机电脑均可用。
- **语言**：中文为主 + 少量英文点缀（导航 / 栏目副标）。
- **代号**：`helloword_sleep`。

## 4. 代表性 AI 对话记录

> 说明：本项目的构思、技术选型、设计、交互等决策均通过与 AI 对话完成。以下为代表性节选，完整记录见本次对话原文。

- **主题取舍**：放弃"学习笔记"主题，因涉及过多个人 / 公司资料，不够尊重也不安全。
- **技术栈辩论**：纯 HTML/CSS/JS（A）vs 框架（C）。结合本人的经验，确认纯栈最利于"理解代码 + 最短发布链路"，且框架栈的后端在 Pages 上跑不了。
- **视觉 / 明暗**：选定干净底 + 卡片 + 靛蓝，并加双主题开关解决 light / dark 偏好分歧。
- **结构 / 交互**：多页 + Playground 独立页 + 手机汉堡菜单。
- **玩具选择**：第一个做数独（自生成算法）；第二个做猜飞机 / 寻机头（单人 vs 电脑），确认其机制明显区别于数独，是独立游戏而非变体。

（此处可贴对话截图或节选，证明"本人如何提问、追问、比较方案、做决定"。）

## 5. 一个自己发现并解决的问题

> 待开发阶段补充。预计在以下环节产生真实问题并记录：
>
> - 数独棋盘生成 / 校验逻辑的 bug；
> - 响应式在手机上的某处显示问题；
> - 明暗切换的对比度或闪烁问题。
>
> 写完代码、遇到并解决一个真实问题后，把「现象 → 排查过程 → 最终解决」补到这里。

## 6. 对 AI 生成代码的理解、修改和取舍

> 待你阅读 `base.css` / `site.js` 等代码后，补充你自己的理解和改动：
>
> - `base.css`：CSS 变量定义明暗双主题、卡片 / 导航 / 响应式样式——说明你理解各部分作用，改了哪、留了哪。
> - `site.js`：主题切换（读取 / 记忆 `localStorage`）+ 汉堡菜单逻辑——说明你怎么读懂并取舍。
> - 各 `.html`：共享导航结构——说明复制时的统一与差异。
>
> 这部分务必你自己写，体现"对 AI 生成代码的理解、修改和取舍"。
