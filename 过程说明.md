# 个人网站 · 过程说明

这个站是我从头做出来的东西——从想做什么、挑技术、写代码，到发布上线，整条路都自己走了一遍。下面按顺序记一下，这一路是怎么想的、怎么定的。

## 0. 项目概览

站点叫 **HelloWorld-Sleep**，是一个「作品集 + 小游戏」合体的小站，用纯 HTML / CSS / JavaScript 写成，托管在 GitHub Pages 上。

- 仓库地址：（部署后补上）
- 网站地址：（部署后补上）

## 1. 我想做一个什么样的站

一开始我其实想做个「学习笔记站」，把学的东西都记下来。后来改了主意：笔记这种东西，写着写着就匮乏了。

所以主题整个换掉了，改成「只展示产出物」：只放做出来的东西，不写私人心路。

定位是**作品集 + 小游戏**的合体站，主要给爱我一样爱鼓捣的人看。

顺带定了几条目标：

- 网站本身，就是我拿得出来的第一个真实作品；
- 借它把 Git / GitHub 整套流程跑熟（初始化 → 提交 → 推送 → Pages 上线）；
- 手机、电脑上都要能正常看。

## 2. 技术栈：为什么用了最"朴素"的纯 HTML/CSS/JS

最后选的是纯 HTML / CSS / JavaScript——没框架、没构建、没后端。

一开始也纠结过要不要上框架（React / Next / Vue 这类）。比下来，纯的更合我：

- **每一行都看得懂**。浏览器跑的就是我写的，没有中间层，改起来心里有底。
- **发布链路最短**。改完文件 `git push`，Pages 直接就上线，中间没有「构建」这一关。
- **Git 干净**。没有 `node_modules`、没有 lockfile、没有构建产物，每次提交就是一句「我改了哪一页」。
- **Pages 上零配置**。静态文件直接托管，不用写 CI、不用配 Action。
- **不容易过时**。没有框架版本升级的麻烦，出问题时看到的就是真实的元素。

静态生成器（Jekyll / Hugo）也想过，但为 5 个页面上模板系统是杀鸡用牛刀，而且离「看懂代码」更远。框架还有个硬伤：它的后端在 Pages 上根本跑不了。

## 3. 页面和交互是怎么定的

站是多页的，一共 5 个页面：

- `index.html`——首页：一句自我介绍 + 四个入口卡片。
- `about.html`——关于：写得简洁清晰。
- `projects.html`——作品：放做过的项目，带源码链接。
- `playground.html`——小游戏：每款游戏一个独立页面。
- `contact.html`——联系：主要留 GitHub。

视觉想要「干净」：浅底 + 卡片 + 一个靛蓝的强调色（`#6366f1`），字体直接用系统自带的——不用加载、最快也最稳。

明暗两套主题做了个开关。第一次进站跟着系统走（`prefers-color-scheme`），之后用 `localStorage` 记住选的那套；实现上靠 CSS 变量，明暗各一套值。

导航和交互上：

- 顶部导航固定在顶上，滚到哪都在。
- 电脑上链接横着排；手机上（≤640px）收进 ☰ 汉堡菜单。
- 小游戏点卡片进独立页面，这样数独的 9×9 棋盘才有整屏空间。
- 右上角 🌓 一键切明暗，切完记住。

响应式用 flexbox / grid 加媒体查询搞定。语言中文为主，掺一点点英文。站的代号就叫 `HelloWorld-Sleep`。

## 4. 过程中和 AI 聊出来的几个决定

构思、选型、设计这些，基本都是和 AI 一来一回聊出来的。挑几个有代表性的记一下：

- **主题**：本来想写学习笔记，AI 提醒笔记容易牵扯到不该公开的内容——于是整个换成「只展示产出」。
- **技术栈**：在「纯 HTML/CSS/JS」和「上框架」之间来回比，最后确认纯栈最利于「看懂代码 + 最短发布链路」，而且框架的后端在 Pages 上跑不了。
- **视觉**：定了干净底 + 卡片 + 靛蓝，再加个明暗开关，把「我偏浅色 / 你偏深色」的分歧直接解决掉。
- **结构**：多页 + 小游戏独立页 + 手机汉堡菜单。
- **游戏**：两个游戏都是我自己想出来的，玩法我定，它负责把我说的一点点写成代码。
  - **数独**：功能是一条条加上去的。计时要能暂停，而且**暂停时必须把题面遮住**——不然暂停就没意义了；填数要用屏幕键盘，而不是让玩家在格子里打字；草稿得是**另一套键盘**，跟正式填的数字分开。难度分简单 / 中等 / 困难三档是它提的，我觉得合理就留下了——这种「它提、我判断」的地方也不少，跟「我提、它实现」正好互补。
  - **寻机头**：整体想法是我的。飞机的造型我自己设计；规则定成——10×10 棋盘藏 2 架，三款造型每局随机抽两款、各自随机朝向，这个「三选二 + 随机朝向」是我制定的。它跟数独机制完全不同，是另一款独立游戏，不是变体。

**对话节选**

来回对话挺长，摘四段最能看出「我怎么问、怎么追问、怎么拍板」的——**AI 的原话带了不少只在当时上下文里有用的细节（涉及我那时的学习内容、类比等），不适合放进公开文档，所以我转写成了"大意"，只留决策逻辑**。

> **主题**
>
> > 我：我想要创建一个个人网站，受众和要展示的内容是什么。你认为我做什么主题好，有没有推荐。  
> > AI：推荐「学习成长 + 作品集混合站」——把学习过程当内容。  
> > 我：第一个推荐不好，**涉及太多的资料，不够尊重**。但是方向很好，受众的设计我很满意。有没有别的推荐。  
> > AI：你说得对——把学习过程摊开写既不体面也不安全。方向（服务同行 / 招聘方 + 作品 + 内容的混合感）和受众设计保留，但主题换成「轻量、只展示产出、不暴露私人信息」的。给你三个替代主题，最推荐「项目/实验展示站」——只放亲手做出来的东西。

> **技术栈**
>
> > 我：先讨论技术栈，为什么你觉得纯 HTML/CSS/JS 最好。有没有又清晰又强硬的理由，然后有没有和其他形式技术栈的对比。  
> > AI：对零基础学习者来说，纯 HTML/CSS/JS 是唯一同时满足「能看懂代码、能讲清选型、发布链路最短」的栈，其他选择都是给自己加一层既用不上、又解释不清的抽象。理由列了 5 条（零抽象、Git 干净、Pages 零配置、不易过时……）。

> **视觉**
>
> > 我：视觉风格上，你认为什么最适合我们设立的网站构想。这个网站需要交互，**极简干净 / 活泼卡片 / 混合哪种好**？不同风格实现难度是不是一致的。  
> > AI：交互是网站的"发动机"（靠 JS），视觉是"外壳"（靠 CSS），两者独立——**风格怎么选不改交互难度**。推荐**极简干净为底 + 卡片式布局 + 单一活泼强调色**——一站两制：作品集/关于页走极简（对招聘方有可信度、好读），Playground 用卡片（显得好玩、鼓励点击）。**你正好有两份"列表"**（项目列表、玩具列表），卡片天然映射，开发省事。

> **寻机头（第二个游戏）**
>
> > 我：「寻机头」就例如在 8×8 的格子里面找到摆放位置不同的飞机，每次只能撕一个格子，机身是蓝色，机头是红色，其余是白色，但是这个游戏原本是两人竞争的，看谁快找到对方放下的飞机，我们的话可以改良。  
> > AI：懂了——寻机头就是海战棋/Battleship 那一类，你描述的"每次翻一格看颜色"完全对得上。它算货真价实的"第二个游戏"，机制跟数独完全不同——数独是"逻辑填数/约束求解"，寻机头是"隐藏信息 + 猜测 + 状态追踪"，放作品集/Playground 里正好显示我会做不同类型的东西。

## 5. 一个我自己发现并解决的问题

开发里踩过两个印象比较深的坑，都是自己撞见的。

**数独：同一个清除键，两种脾气。**

同一格里，用正常模式填进去的数字，切到草稿模式后按清除键删不掉；可草稿模式做的小标记（候选数），两种模式下都能删。同一个格子、同一个清除键，两种内容却两种脾气，用着很别扭。

顺着「清除」这条路径把代码捋下去，问题出在 `inputNumber(0)`：它按模式走两个分支，正常模式会清掉 `puzzle` 里的数字，草稿模式却只清了 `notes`（候选数），漏了 `puzzle`。补上 `puzzle[selectedIdx] = 0;`，让两边一样把数字和标记一起清掉，就正常了。

**寻机头：格子会「变形」。**

棋盘是 10×10 的格子。玩着玩着撞见一件怪事：点到空地（击空）之后，那个格子的**宽没变、高却被撑长了**——正方形变成了竖长方形；更邪门的是，只要再去点它旁边的格子，它又「啪」地弹回正方形。

击空的格子本来就要画一个小点（这是设计好的），所以问题不在那个点，而在**格子没被「锁死」成正方形**：格子里一多出内容，高度就被顶大了；旁边格子被点击时触发一次重排，于是又弹回去。修法是给格子写死「必须正方形」——样式里加上 `aspect-ratio: 1 / 1`，这样不管里面塞什么，都撑不歪。改完之后，怎么点都不变形了。

## 6. 对 AI 生成代码的理解、修改和取舍

**明暗主题这条线，我算是从头到尾捋明白了——也第一次真切体会到，"读懂代码"和"能改代码"是两件不一样的事。**

一开始我以为"切主题"是件很麻烦的事：两套颜色，岂不是得写两份样式、挨个元素改？看懂之后才发现，它做得非常轻，一共就三层。

**第一层，HTML。** 整站的"根"是 `<html>` 这个标签，所有内容都挂在它底下。

**第二层，CSS（`base.css`）。** 开头 `:root` 里定义了一套颜色变量——背景、卡片、文字、边框、强调色，全是 `--` 开头的；紧接着 `[data-theme="dark"]` 里，又定义了一套**同名**的变量，值是深色那套。页面上所有元素取色，用的都是 `var(--xxx)`——读作"按这个名字去取值"：名字和值是分开的两样东西，`--bg` 只是个代号，它现在指向哪个颜色，取出来就是哪个。

关键就在这儿：**只要给 `<html>` 挂上 `data-theme="dark"`，整站的颜色就一起换成深色那套**——因为所有元素都在 `<html>` 底下，读的是同一批变量，一个具体元素都不用碰。这是我最佩服的一环：它把"颜色"这件事收在了**一个地方**，以后想调色，只改开头那两组变量就行，不用满文件找。

**第三层，JS（`site.js`）。** 它负责决定"该挂哪套"：

- 打开页面时——**先问记忆，没有就问系统**：先读 `localStorage` 里存过的选择；没有，就看系统是不是深色（`prefers-color-scheme`）。
- 点 🌓 时——把当前主题取反、挂上去，再写回 `localStorage`。所以下次打开，还是你选的那套。

**我的取舍：这段代码我一行都没动。**

不是没时间改，是**找不到该改的地方**。要是让我自己从零写，我大概会老老实实写两份样式表——又长、又容易漏改，改完还不敢保证两套颜色对得上。它这种"变量换色 + 根标签挂属性"的做法，已经把改动面压到了最小，我没有理由去动它。

这给我留下一条判断：**能看懂、且已经足够好的地方，就留着不动，不为了改而改。** 改代码的价值不在"改了多少"，而在"改得对不对"。

**HTML 那边的另一个取舍：能共用的共用，该分开的分开。**

五个主页面的导航栏和页脚是一模一样的，做法就是把同一段 HTML 复制过去、只改一下"哪个是当前页"。游戏页则不一样——它们不需要整个菜单，只要一个"← 返回"加主题开关，所以用的是另一套更简单的导航。我的理解是：不硬凑一个统一模板，也不给每个页面重复造一套，**该共用的就复制，该分开的就分开**——对这么小的站来说，这是最省事、也最好懂的做法。

整个站我真正动过手的，一是文案，二是**两个游戏的玩法**（第 4 节写过）；代码结构上，只有第 5 节那两个坑是我动手改的。剩下的我读懂了，然后选择留下。
